'use strict';

// Intentionally idle in v0. Everything happens in content scripts; this file
// exists so v1 features (reset notifications, chrome.action badge) have a
// stable home without a manifest change.
